services:
  api:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: ${CONTAINER_NAME}

    ports:
      - "${SERVER_PORT}:${SERVER_PORT}"

    env_file:
      - .env

    volumes:
      - ./app:/app

    restart: always


