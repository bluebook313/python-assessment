services:
  api:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: ${CONTAINER_NAME}

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - ./app:/app

    restart: always


