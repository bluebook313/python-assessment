services:
  api:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: shopping-project

    ports:
      - "8000:8000"

    env_file:
      - .env

    # volumes:
    #   - .:/app
    #   # keep installed deps from being shadowed by the bind mount
    #   - /app/.venv


    restart: always


